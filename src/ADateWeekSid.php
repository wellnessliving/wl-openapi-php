<?php
namespace WlSdk;

/**
 * A class for the days of the week.
 * 
 * Values:
 * - 5 (`FRIDAY`): Friday.
 * - 1 (`MONDAY`): Monday.
 * - 6 (`SATURDAY`): Saturday.
 * - 7 (`SUNDAY`): Sunday.
 * - 4 (`THURSDAY`): Thursday.
 * - 2 (`TUESDAY`): Tuesday.
 * - 3 (`WEDNESDAY`): Wednesday.
 */
class ADateWeekSid
{
    /** Friday. */
    const FRIDAY = 5;
    /** Monday. */
    const MONDAY = 1;
    /** Saturday. */
    const SATURDAY = 6;
    /** Sunday. */
    const SUNDAY = 7;
    /** Thursday. */
    const THURSDAY = 4;
    /** Tuesday. */
    const TUESDAY = 2;
    /** Wednesday. */
    const WEDNESDAY = 3;
}
