<?php

namespace WlSdk\Wl\Page\Map;

/**
 * List of possible left navigation panels.
 *
 * Last used ID: 18.
 *
 * Values:
 * - 13 (`ACHIEVE`): Pages of achieve application.
 * - 15 (`COLLECTION`): Collection left menu panel.
 * - 7 (`DASHBOARD`): Dashboard left menu panel.
 * - 14 (`FINANCE`): Finance left menu panel.
 * - 16 (`FITBUILDER`): Fit Builder left menu panel.
 * - 5 (`MAIL`): Direct mail.
 * - 18 (`MARKETING`): Pages of marketing campaigns.
 * - 6 (`POWER`): Power search filter panel.
 * - 12 (`PRESENCE`): Pages of custom websites.
 * - 1 (`PROFILE`): Pages of client's profile.
 * - 11 (`QUIZ`): Forms feature left menu panel.
 * - 2 (`REPORT`): Different report categories and reports.
 * - 4 (`REWARD`): List of pages to reward program.
 * - 3 (`SETUP`): List of pages to setup business.
 * - 8 (`SMS`): 2-way SMS.
 * - 17 (`SUPPORT`): Help left menu panel.
 * - 9 (`VIDEO`): Video setup.
 * - 10 (`VIRTUAL`): Virtual setup.
 */
class MapSid
{
    /** Pages of achieve application. */
    public const ACHIEVE = 13;

    /** Collection left menu panel. */
    public const COLLECTION = 15;

    /** Dashboard left menu panel. */
    public const DASHBOARD = 7;

    /** Finance left menu panel. */
    public const FINANCE = 14;

    /** Fit Builder left menu panel. */
    public const FITBUILDER = 16;

    /** Direct mail. */
    public const MAIL = 5;

    /** Pages of marketing campaigns. */
    public const MARKETING = 18;

    /** Power search filter panel. */
    public const POWER = 6;

    /** Pages of custom websites. */
    public const PRESENCE = 12;

    /** Pages of client's profile. */
    public const PROFILE = 1;

    /** Forms feature left menu panel. */
    public const QUIZ = 11;

    /** Different report categories and reports. */
    public const REPORT = 2;

    /** List of pages to reward program. */
    public const REWARD = 4;

    /** List of pages to setup business. */
    public const SETUP = 3;

    /** 2-way SMS. */
    public const SMS = 8;

    /** Help left menu panel. */
    public const SUPPORT = 17;

    /** Video setup. */
    public const VIDEO = 9;

    /** Virtual setup. */
    public const VIRTUAL = 10;
}
