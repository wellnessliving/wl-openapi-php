<?php
namespace WlSdk\Wl\Member\Group;

/**
 * Shapes of client group icons.
 * 
 * Last used ID: 7.
 * 
 * Values:
 * - 1 (`CIRCLE`): Circle.
 * - 2 (`HEXAGON`): Hexagon.
 * - 3 (`OVAL`): Oval.
 * - 4 (`PENTAGON`): Pentagon.
 * - 5 (`RECTANGLE`): Rectangle.
 * - 6 (`SQUARE`): Square.
 * - 7 (`STAR`): Star.
 */
class ShapeSid
{
    /** Circle. */
    const CIRCLE = 1;
    /** Hexagon. */
    const HEXAGON = 2;
    /** Oval. */
    const OVAL = 3;
    /** Pentagon. */
    const PENTAGON = 4;
    /** Rectangle. */
    const RECTANGLE = 5;
    /** Square. */
    const SQUARE = 6;
    /** Star. */
    const STAR = 7;
}
