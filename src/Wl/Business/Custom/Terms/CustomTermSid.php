<?php

namespace WlSdk\Wl\Business\Custom\Terms;

/**
 * Term slots available on the Custom Terms settings page.
 *
 * Last used ID: 5.
 *
 * Values:
 * - 2 (`APPOINTMENT`): Appointment.
 * - 5 (`BUSINESS`): Business.
 * - 3 (`CLASS_TERM`): Class.
 * - 4 (`CLIENT`): Client.
 * - 1 (`STAFF`): Staff.
 */
class CustomTermSid
{
    /** Appointment. */
    public const APPOINTMENT = 2;

    /** Business. */
    public const BUSINESS = 5;

    /** Class. */
    public const CLASS_TERM = 3;

    /** Client. */
    public const CLIENT = 4;

    /** Staff. */
    public const STAFF = 1;
}
