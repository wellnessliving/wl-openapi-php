<?php

namespace WlSdk\Wl\Integration\Curves;

/**
 * Curves deal type.
 *
 * Last used ID: 5
 *
 * Values:
 * - 1 (`ALL`): All.
 * - 2 (`DISCOUNT`): Discount.
 * - 3 (`EMBED`): Embed.
 * - 4 (`PROMOTE`): Promote.
 * - 5 (`SUBSIDIZE`): Subsidize.
 */
class DealTypeSid
{
    /** All. */
    public const ALL = 1;

    /** Discount. */
    public const DISCOUNT = 2;

    /** Embed. */
    public const EMBED = 3;

    /** Promote. */
    public const PROMOTE = 4;

    /** Subsidize. */
    public const SUBSIDIZE = 5;
}
