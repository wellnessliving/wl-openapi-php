<?php

namespace WlSdk\Wl\Profile\Page\Overview;

/**
 * List of profile widgets in client's Overview page.
 *
 * Last used ID: 16.
 *
 * Values:
 * - 1 (`ACTIVITY`): Activity widget.
 * - 2 (`ALERTS`): Alerts widget.
 * - 3 (`ATTENDANCE`): Attendance widget.
 * - 4 (`BELTS`): Belts widget.
 * - 5 (`CLIENT_GROUPS`): Client groups widget.
 * - 6 (`CLIENT_INFO`): Client info widget.
 * - 7 (`CURVES_IDS`): Curves Ids widget.
 * - 8 (`FORMS`): Forms widget.
 * - 9 (`NOTES`): Notes widget.
 * - 10 (`PURCHASE_OPTIONS`): Purchase options widget.
 * - 11 (`QUICK_CHECK_IN`): Quick check-in for today widget.
 * - 12 (`RELATIONSHIPS`): Relationships widget.
 * - 13 (`REWARDS`): Rewards widget.
 * - 14 (`SUMMARY_CARDS`): Summary cards widget.
 * - 15 (`TASKS`): Tasks widget.
 * - 16 (`TUITION`): Tuition widget.
 */
class ProfileWidgetSid
{
    /** Activity widget. */
    public const ACTIVITY = 1;

    /** Alerts widget. */
    public const ALERTS = 2;

    /** Attendance widget. */
    public const ATTENDANCE = 3;

    /** Belts widget. */
    public const BELTS = 4;

    /** Client groups widget. */
    public const CLIENT_GROUPS = 5;

    /** Client info widget. */
    public const CLIENT_INFO = 6;

    /** Curves Ids widget. */
    public const CURVES_IDS = 7;

    /** Forms widget. */
    public const FORMS = 8;

    /** Notes widget. */
    public const NOTES = 9;

    /** Purchase options widget. */
    public const PURCHASE_OPTIONS = 10;

    /** Quick check-in for today widget. */
    public const QUICK_CHECK_IN = 11;

    /** Relationships widget. */
    public const RELATIONSHIPS = 12;

    /** Rewards widget. */
    public const REWARDS = 13;

    /** Summary cards widget. */
    public const SUMMARY_CARDS = 14;

    /** Tasks widget. */
    public const TASKS = 15;

    /** Tuition widget. */
    public const TUITION = 16;
}
