<?php

namespace WlSdk\Wl\Resource\Image;

/**
 * Predefined icons for assets.
 *
 * Last used ID: 29.
 *
 * Values:
 * - 1 (`BENCH`): Training bench.
 * - 2 (`BIKE_1`): Exercise bike.
 * - 3 (`BIKE_2`): Exercise bike.
 * - 4 (`BIKE_3`): Exercise bike.
 * - 5 (`BIKE_4`): Exercise bike.
 * - 6 (`BIKE_5`): Exercise bike.
 * - 7 (`BIKE_6`): Exercise bike.
 * - 8 (`BOOT`): Boot.
 * - 9 (`DOOR`): Door.
 * - 10 (`FAN_1`): Fan.
 * - 11 (`FAN_2`): Fan.
 * - 12 (`MAN`): Man.
 * - 13 (`MAT`): Mat.
 * - 14 (`MIRROR`): Mirror.
 * - 15 (`ORBITRACK_1`): Orbitrack.
 * - 16 (`ORBITRACK_2`): Orbitrack.
 * - 17 (`ORBITRACK_3`): Orbitrack.
 * - 18 (`ORBITRACK_4`): Orbitrack.
 * - 19 (`ORBITRACK_5`): Orbitrack.
 * - 20 (`ORBITRACK_6`): Orbitrack.
 * - 21 (`PUNCH_1`): Boxing punch.
 * - 22 (`PUNCH_2`): Boxing punch.
 * - 23 (`RECTANGLE`): Rectangle.
 * - 24 (`SPEAKER`): Loudspeaker.
 * - 25 (`TREADMILL_1`): Treadmill.
 * - 26 (`TREADMILL_2`): Treadmill.
 * - 27 (`TV`): TV.
 * - 28 (`TWINE`): Twines.
 * - 29 (`WEIGHT`): Weight.
 */
class ImageIconSid
{
    /** Training bench. */
    public const BENCH = 1;

    /** Exercise bike. */
    public const BIKE_1 = 2;

    /** Exercise bike. */
    public const BIKE_2 = 3;

    /** Exercise bike. */
    public const BIKE_3 = 4;

    /** Exercise bike. */
    public const BIKE_4 = 5;

    /** Exercise bike. */
    public const BIKE_5 = 6;

    /** Exercise bike. */
    public const BIKE_6 = 7;

    /** Boot. */
    public const BOOT = 8;

    /** Door. */
    public const DOOR = 9;

    /** Fan. */
    public const FAN_1 = 10;

    /** Fan. */
    public const FAN_2 = 11;

    /** Man. */
    public const MAN = 12;

    /** Mat. */
    public const MAT = 13;

    /** Mirror. */
    public const MIRROR = 14;

    /** Orbitrack. */
    public const ORBITRACK_1 = 15;

    /** Orbitrack. */
    public const ORBITRACK_2 = 16;

    /** Orbitrack. */
    public const ORBITRACK_3 = 17;

    /** Orbitrack. */
    public const ORBITRACK_4 = 18;

    /** Orbitrack. */
    public const ORBITRACK_5 = 19;

    /** Orbitrack. */
    public const ORBITRACK_6 = 20;

    /** Boxing punch. */
    public const PUNCH_1 = 21;

    /** Boxing punch. */
    public const PUNCH_2 = 22;

    /** Rectangle. */
    public const RECTANGLE = 23;

    /** Loudspeaker. */
    public const SPEAKER = 24;

    /** Treadmill. */
    public const TREADMILL_1 = 25;

    /** Treadmill. */
    public const TREADMILL_2 = 26;

    /** TV. */
    public const TV = 27;

    /** Twines. */
    public const TWINE = 28;

    /** Weight. */
    public const WEIGHT = 29;
}
