<?php

namespace WlSdk\Core\Drive;

/**
 * List of image types.
 *
 * Values:
 * - 4 (`BMP`): Bmp image.
 * - 1 (`GIF`): Gif image.
 * - 2 (`JPEG`): Jpeg image.
 * - 3 (`PNG`): Png image.
 */
class DriveTypeSid
{
    /** Bmp image. */
    public const BMP = 4;

    /** Gif image. */
    public const GIF = 1;

    /** Jpeg image. */
    public const JPEG = 2;

    /** Png image. */
    public const PNG = 3;
}
