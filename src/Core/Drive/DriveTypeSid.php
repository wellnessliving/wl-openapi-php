<?php
namespace WlSdk\Core\Drive;

/**
 * List of image types.
 * 
 * Values:
 * - 4 (`BMP`): Bmp image.
 * - 1 (`GIF`): Gif image.
 * - 2 (`JPEG`): Jpeg image.
 * - 3 (`PNG`): Png image.
 */
class DriveTypeSid
{
    /** Bmp image. */
    const BMP = 4;
    /** Gif image. */
    const GIF = 1;
    /** Jpeg image. */
    const JPEG = 2;
    /** Png image. */
    const PNG = 3;
}
