<?php

namespace WlSdk\Core\Sms\A2p;

/**
 * Contact job positions for A2P10DLC registration.
 *
 * Last used id: 6.
 *
 * Values:
 * - 1 (`CEO`): CEO.
 * - 2 (`CFO`): CFO.
 * - 3 (`DIRECTOR`): Director.
 * - 4 (`GENERAL_COUNSEL`): General Counsel.
 * - 5 (`GM`): GM.
 * - 6 (`VP`): VP.
 */
class ContactJobPositionSid
{
    /** CEO. */
    public const CEO = 1;

    /** CFO. */
    public const CFO = 2;

    /** Director. */
    public const DIRECTOR = 3;

    /** General Counsel. */
    public const GENERAL_COUNSEL = 4;

    /** GM. */
    public const GM = 5;

    /** VP. */
    public const VP = 6;
}
