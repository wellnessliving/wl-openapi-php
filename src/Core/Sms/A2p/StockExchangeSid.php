<?php

namespace WlSdk\Core\Sms\A2p;

/**
 * Stock exchanges for A2P10DLC registration.
 *
 * Last used id: 27.
 *
 * Values:
 * - 1 (`AMEX`): American Stock Exchange.
 * - 2 (`AMX`): Armenia Securities Exchange.
 * - 3 (`ASX`): Australian Securities Exchange.
 * - 4 (`B3`): Brazil Stock Exchange.
 * - 5 (`BME`): Bolsas y Mercados Españoles.
 * - 6 (`BSE`): Bombay Stock Exchange.
 * - 7 (`FRA`): Frankfurt Stock Exchange.
 * - 8 (`ICEX`): Indian Commodity Exchange.
 * - 9 (`JPX`): Japan Exchange Group.
 * - 10 (`JSE`): Jamaica Stock Exchange.
 * - 11 (`KRX`): Korea Exchange.
 * - 12 (`LON`): London Stock Exchange.
 * - 13 (`NASDAQ`): NASDAQ.
 * - 14 (`NONE`): No Stock Exchange.
 * - 15 (`NSE`): National Stock Exchange of India Ltd.
 * - 16 (`NYSE`): New York Stock Exchange.
 * - 17 (`OMX`): NASDAQ Stockholm.
 * - 18 (`OTHER`): Other Stock Exchange.
 * - 19 (`SEHK`): Stock Exchange of Hong Kong.
 * - 20 (`SGX`): Singapore Exchange Limited.
 * - 21 (`SSE`): Shanghai Stock Exchange.
 * - 22 (`STO`): Stockholm Stock Exchange.
 * - 23 (`SWX`): Swiss Stock Exchange.
 * - 24 (`SZSE`): Shenzhen Stock Exchange.
 * - 25 (`TSX`): Toronto Stock Exchange.
 * - 26 (`TWSE`): Taiwan Stock Exchange.
 * - 27 (`VSE`): Vadodara Stock Exchange Limited.
 */
class StockExchangeSid
{
    /** American Stock Exchange. */
    public const AMEX = 1;

    /** Armenia Securities Exchange. */
    public const AMX = 2;

    /** Australian Securities Exchange. */
    public const ASX = 3;

    /** Brazil Stock Exchange. */
    public const B3 = 4;

    /** Bolsas y Mercados Españoles. */
    public const BME = 5;

    /** Bombay Stock Exchange. */
    public const BSE = 6;

    /** Frankfurt Stock Exchange. */
    public const FRA = 7;

    /** Indian Commodity Exchange. */
    public const ICEX = 8;

    /** Japan Exchange Group. */
    public const JPX = 9;

    /** Jamaica Stock Exchange. */
    public const JSE = 10;

    /** Korea Exchange. */
    public const KRX = 11;

    /** London Stock Exchange. */
    public const LON = 12;

    /** NASDAQ. */
    public const NASDAQ = 13;

    /** No Stock Exchange. */
    public const NONE = 14;

    /** National Stock Exchange of India Ltd. */
    public const NSE = 15;

    /** New York Stock Exchange. */
    public const NYSE = 16;

    /** NASDAQ Stockholm. */
    public const OMX = 17;

    /** Other Stock Exchange. */
    public const OTHER = 18;

    /** Stock Exchange of Hong Kong. */
    public const SEHK = 19;

    /** Singapore Exchange Limited. */
    public const SGX = 20;

    /** Shanghai Stock Exchange. */
    public const SSE = 21;

    /** Stockholm Stock Exchange. */
    public const STO = 22;

    /** Swiss Stock Exchange. */
    public const SWX = 23;

    /** Shenzhen Stock Exchange. */
    public const SZSE = 24;

    /** Toronto Stock Exchange. */
    public const TSX = 25;

    /** Taiwan Stock Exchange. */
    public const TWSE = 26;

    /** Vadodara Stock Exchange Limited. */
    public const VSE = 27;
}
