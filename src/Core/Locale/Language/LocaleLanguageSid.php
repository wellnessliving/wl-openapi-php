<?php

namespace WlSdk\Core\Locale\Language;

/**
 * A list of all languages.
 *
 * 5,7 are not used, may be used for other languages.
 *
 * Last used id: 9.
 *
 * Values:
 * - 6 (`DE`): German.
 * - 2 (`EN`): English.
 * - 9 (`ES`): Spanish.
 * - 4 (`FR`): French.
 * - 5 (`PT`): Portuguese.
 * - 1 (`RU`): Russian.
 * - 8 (`TR`): Turkish.
 * - 3 (`UK`): Ukrainian.
 */
class LocaleLanguageSid
{
    /** German. */
    public const DE = 6;

    /** English. */
    public const EN = 2;

    /** Spanish. */
    public const ES = 9;

    /** French. */
    public const FR = 4;

    /** Portuguese. */
    public const PT = 5;

    /** Russian. */
    public const RU = 1;

    /** Turkish. */
    public const TR = 8;

    /** Ukrainian. */
    public const UK = 3;
}
