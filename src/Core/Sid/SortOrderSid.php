<?php
namespace WlSdk\Core\Sid;

/**
 * List of possible sort order.
 * 
 * Values:
 * - 2 (`ASC`): Ascending order.
 * - 1 (`DESC`): Descending order.
 */
class SortOrderSid
{
    /** Ascending order. */
    const ASC = 2;
    /** Descending order. */
    const DESC = 1;
}
