<?php
namespace WlSdk;

/**
 * Day time periods.
 * 
 * Values:
 * - 2 (`AFTERNOON`): Afternoon.
 * - 3 (`EVENING`): Evening.
 * - 1 (`MORNING`): Morning.
 */
class RsScheduleTimeSid
{
    /** Afternoon. */
    const AFTERNOON = 2;
    /** Evening. */
    const EVENING = 3;
    /** Morning. */
    const MORNING = 1;
}
