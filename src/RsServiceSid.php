<?php
namespace WlSdk;

/**
 * Identifiers for services types.
 * 
 * Last used ID: 5.
 * 
 * Values:
 * - 1 (`APPOINTMENT`): Appointments.
 * - 2 (`CLASSES`): Classes.
 * - 3 (`ENROLLMENT`): Enrolments.
 * - 5 (`RESOURCE`): Resources.
 * - 4 (`VISIT`): Visits.
 */
class RsServiceSid
{
    /** Appointments. */
    const APPOINTMENT = 1;
    /** Classes. */
    const CLASSES = 2;
    /** Enrolments. */
    const ENROLLMENT = 3;
    /** Resources. */
    const RESOURCE = 5;
    /** Visits. */
    const VISIT = 4;
}
