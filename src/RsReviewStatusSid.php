<?php
namespace WlSdk;

/**
 * Review status identifiers.
 * 
 * Values:
 * - 1 (`ADMIN`): Admin.
 * - 4 (`HIDDEN`): Hidden.
 * - 3 (`PUBLISH`): Publish.
 */
class RsReviewStatusSid
{
    /** Admin. */
    const ADMIN = 1;
    /** Hidden. */
    const HIDDEN = 4;
    /** Publish. */
    const PUBLISH = 3;
}
